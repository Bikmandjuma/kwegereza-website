VITE_API_URL=http://localhost:4000/api

# From Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0
# Client IDs -> Web application. Add your dev and prod origins under
# "Authorized JavaScript origins" (no path, no trailing slash).
VITE_GOOGLE_CLIENT_ID=REPLACE_WITH_YOUR_GOOGLE_CLIENT_ID
